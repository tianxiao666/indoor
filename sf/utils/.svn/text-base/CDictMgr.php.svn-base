<?php
/**
 * 
 *
 */

class CDictMgr
{
	static  function toHtmlOptions($data)
	{
		$options = array();
		if (is_array($data)) {
			foreach ($data as $value) {
				$options[$value['OPTION_VALUE']] = $value['OPTION_NAME'];
			}
		}
		return $options;
	}
	
	
	
	
	
}
